# DECISIONS.md

| Date | Decision | Basis |
|------|----------|-------|
| 12 Sep 2026 | Adopt the portable project-state governance pattern (AGENTS.md at repo root + `docs/media-001/` state files) for MEDIA-001 | Per CC handoff spec — prevents dependence on any single agent's conversation history |
| 12 Sep 2026 | Preserve `audio/master/The_R2_podcast.m4a` as immutable master; any processing produces a documented derivative under `audio/derivatives/` | Per CC handoff Section 7 |
| 12 Sep 2026 | Do not regenerate the podcast merely because it is AI-generated | Explicit instruction in CC handoff Section 0 |
| 12 Sep 2026 | Reconciled against live qzenta/sikatrix repo before writing anything: repo had no pre-existing AGENTS.md/governance docs, so this bootstrap creates rather than merges | Per CC handoff Section 5 ("avoid overwriting useful existing governance") — confirmed nothing existed to overwrite |
| 12 Sep 2026 | Governance docs placed in `docs/media-001/` (repo already has a `docs/` convention: `LINKEDIN_SETUP.md`, `X_SETUP.md`); `AGENTS.md` alone kept at repo root per its conventional discovery location | Matches existing repo convention rather than inventing a new one |
| 12 Sep 2026 | Did not attempt automated transcription to identify the source articles before asking Daniel | Would require downloading a model file without prior approval; left as a human-confirmed action instead of a guess, per CC handoff Section 9 |
| 12 Sep 2026 | Daniel confirmed the two source articles: `vat-registration-threshold-voluntary-growing-sme` and `sars-automatic-vat-registration-2026` | Explicit human confirmation, per Section 9 requirement not to guess |
| 12 Sep 2026 | Downloaded `ggml-small.en.bin` (~465MB whisper.cpp model, from huggingface.co/ggerganov/whisper.cpp) to transcribe the podcast locally | Explicit-permission action; asked Daniel first (filename/source/size), approved before downloading |
| 12 Sep 2026 | Content QA (Section 10) run against the confirmed sources; result treated as a stop condition, not a checklist item, and publication work halted | Findings are material (a factual contradiction + two omitted core facts + a deadline drift) — see CONTENT_QA.md and AGENTS.md's "STOP, DOCUMENT, ESCALATE, DO NOT PUBLISH" rule |
| 12 Sep 2026 | Daniel chose to regenerate the audio (v2) rather than accept v1's findings as editorial simplification; branch held unmerged, no distribution prep, pending v2 | Explicit human decision on the human-judgement call this worker deliberately left open |
| 12 Sep 2026 | v2 supplied; ran a full independent technical + content QA pass rather than diffing against v1's findings, per Daniel's explicit instruction | Matches the "treat as new, unverified master" instruction already committed on this branch |
| 12 Sep 2026 | v2 Content QA: PASS — all 4 v1 material findings resolved, no new discrepancies found; cleared to proceed toward Section 33 items | See CONTENT_QA_V2.md / AUDIO_QA_V2.md. Both v1 and v2 masters kept in `audio/master/`; v1 retained as historical/failed-QA record, not deleted |
| 12 Sep 2026 | Show name: "Sikatrix Resources." RSS owner email: `info@sikatrix.com` | Daniel's explicit confirmation, resolving the two open items in PODCAST_IDENTITY.md |
| 12 Sep 2026 | Searched beyond `public/`/Downloads for a higher-res/vector source before accepting the artwork gap; found genuine vector SVGs in the "Sikatrix Design System" stationery export folder | Per Daniel's explicit instruction — check before concluding no source exists |
| 12 Sep 2026 | Downloaded the official open-source Manrope variable font (Google Fonts/GitHub, ~165KB) to render the vector source correctly, rather than accept a silent generic-font substitution | Explicit-permission action; asked Daniel first (filename/source/size), approved before downloading |
| 12 Sep 2026 | Composited a candidate podcast cover (2000×2000, RGB, no alpha) directly from the vector SVG source + correct font | **Superseded same day** — see next row |
| 12 Sep 2026 | Daniel corrected: that vector source is a flat, simplified variant that doesn't match the actual Sikatrix brand mark (the real mark is embossed/beveled, on white, with a curved gold ribbon). Candidate cover removed from the repo (recoverable via git history at commit 9c82f4a) | Fidelity to the real brand matters more than technically meeting the resolution requirement from the wrong source. Searched further per Daniel's instruction (all 5 Design System folder copies + OneDrive-wide search for .ai/.psd/.fig/.sketch/3d/bevel files) — no higher-res or vector source of the correct bevel style exists anywhere accessible. Per the standing instruction, did not upscale the 800×800 reference; reported back with 3 options instead |
| 12 Sep 2026 | **Deferred podcast-directory artwork resolution (option 3): not resolved now, revisit before actual Spotify/Apple submission** | The 1400×1400 minimum is a platform-submission-time requirement — it doesn't gate any of the current draft/prep work, so there's no reason to force a decision on options 1 or 2 today. Recreation (option 1) depends on a human step outside this worker's reach — contacting the original designer for a working source file — which isn't CC's to pursue; approximating it (option 2) is worth doing only once Daniel wants to spend the review cycle on a programmatic fidelity check, not preemptively. Revisiting at actual-submission time means the decision gets made against real deadline pressure once, not twice |
| 12 Sep 2026 | Used `sikatrix_profile_picture_800x800.png` as-is for the **website** episode page (copied to `public/photos/podcast/vat-thresholds-2026-what-changed-cover.png`), while podcast-directory art stays deferred | The two have different requirements: the website has no 1400px minimum, so the correctly-proportioned 800×800 bevel-style asset is fine there even though it doesn't clear the podcast-directory bar. No upscaling involved — used at native resolution |
| 12 Sep 2026 | Editorial/human listening QA of v2 recorded as confirmed, per Daniel's direct statement in chat that he already performed it verbally | Communicated directly by the human owner in the one channel available this session — logged as his assertion, not independently verified by this worker. Does **not** by itself authorize publication or any distribution-boundary action, which remain separately gated (Section 25: passing one gate never implies the next) |
| 12 Sep 2026 | Built the actual `/resources/podcast` (index) and `/resources/podcast/[slug]` (episode) routes in code, following the `content/posts/`/`lib/blog.ts` pattern exactly (new `content/podcast/`, `lib/podcast.ts`, `components/podcast/PodcastPlayer.tsx`) | Explicit instruction: code change on the isolated branch, not a deployment. Verified locally (dev server, screenshots, `tsc --noEmit`) before reporting — no existing files modified, only new ones added |
| 12 Sep 2026 | Added an "Internal preview — not published (MEDIA-001)" banner and `robots: noindex,nofollow` to the episode page metadata | Publication is still not authorized; the code existing on this branch shouldn't be mistakable for a live page even if someone previews the branch directly |
| 12 Sep 2026 | Fixed a real rendering bug found during verification: the shared `ArticleContent` component's list renderer doesn't run inline markdown-link parsing on list items, so a `## Source Articles` markdown list in the episode body rendered as raw unparsed text. Removed that section from the markdown body instead of patching the shared component | The page already has a properly-styled "Source articles" section built directly into `[slug]/page.tsx` using real `Post` objects — the markdown-list version was redundant as well as broken. Fixing by removing the duplicate avoids touching shared component code used across all 36 existing blog posts, per AGENTS.md scope discipline (modify only what's necessary) |
| 12 Sep 2026 | Merged PR #20 into `master` on Daniel's explicit authorization; verified the resulting deployment was `target: production` and actually live at `www.sikatrix.com` (not just a preview) before proceeding | Daniel's message was explicit: "This is Daniel's explicit authorization for this production deployment." Verified rather than assumed, per the standing "check the target before claiming a site is live" practice |
| 12 Sep 2026 | Self-hosted the audio (`public/podcast/`) and built a real RSS feed (`app/resources/podcast/feed.xml/route.ts`) rather than creating an account with a third-party podcast host (Buzzsprout/Transistor/Anchor/etc.) | Daniel's instruction named account-boundary work (Spotify/Apple/YouTube) as explicitly off-limits, and "External accounts" is a standing prohibition (AGENTS.md) without a specific service named. Self-hosting via the existing Vercel/Next.js deployment achieves "audio hosting decision + implementation" and a working, validatable RSS feed with zero new accounts or credentials — the more conservative reading of an ambiguous instruction |
| 12 Sep 2026 | Gave the RSS `<pubDate>` a real date (`rssGeneratedDate: 2026-09-12`, independent of the content's `publishDate` field, which stays `null`) | RSS feeds are not meaningfully "valid" without a pubDate, and Daniel explicitly asked to "build and validate" the feed. Kept it a separate field from `publishDate` (which still gates the website's "Not yet published" display) so the two different gates — RSS technical readiness vs. actual public announcement — don't get conflated |
| 12 Sep 2026 | Found and fixed a second real bug during this pass: the episode markdown body rendered with literal `## Heading` text instead of parsed `<h2>` elements when viewed on the local dev server | Root cause: this machine's `core.autocrlf=true` re-materializes the git-stored LF-only content as CRLF on checkout, and the shared `ArticleContent` component's block-splitter regex (`/\n{2,}/`) doesn't match blank lines encoded as `\r\n\r\n`. Confirmed via `git show` that the actual committed blob (and what Vercel's Linux build deploys) is LF-only and unaffected — this was a local-checkout-only artifact, not a production bug. Fixed by normalizing the local working-tree file back to LF (not by touching git config, which is off-limits, and not by patching the shared component, which isn't broken for what's actually deployed) |
| 12 Sep 2026 | Continuing this work on a new branch (`media-001-audio-rss`) off the now-updated `master`, rather than committing straight to `master` | Matches the pattern already established and explicitly approved this session (branch → PR → preview → explicit merge authorization) for every prior production-facing change. Making the real audio file and RSS feed publicly live is at least as consequential as the episode page itself, so it gets the same review checkpoint rather than skipping straight to master on the assumption that continued instructions imply skipping review |
